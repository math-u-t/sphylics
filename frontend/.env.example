# Flexio Frontend Environment Variables

# API Base URL (Workers API endpoint)
# Development: http://localhost:8787
# Production: https://your-workers-subdomain.workers.dev
VITE_API_BASE_URL=http://localhost:8787

# OAuth 2.0 Configuration (RFC 6749 + RFC 7636 PKCE)
# These settings configure the OAuth 2.0 authorization code flow with PKCE

# OAuth Client ID - Register your application to get this
VITE_OAUTH_CLIENT_ID=flexio-web-client

# OAuth Redirect URI - Must match the registered callback URL exactly
# Development: http://localhost:5173/oauth/callback
# Production: https://yourdomain.com/oauth/callback
VITE_OAUTH_REDIRECT_URI=http://localhost:5173/oauth/callback

# OAuth Scopes - Space-separated list of requested permissions
# Available scopes: read write admin
VITE_OAUTH_SCOPE=read write

# Feature Flags
VITE_ENABLE_OAUTH=true
